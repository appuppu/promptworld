# Prompt World

A black & white minimal physics sandbox platform — hacking together the
system structure of Mario Maker and the UGC explosion of Roblox, where
players use their own AI (Claude) to invent custom physics rules and
build stages.

## Core Concept

- **Stages are data, not code.** Every stage deploys as a lightweight JSON
  text document: time limit, goal conditions, block layout, and
  trigger-and-action physics rules. The game client is an interpreter.
- **Create on web, play on mobile.** Stages are built in a browser (WebGL)
  editor, then shared as a URL. Tapping the link deep-links (Universal
  Links) into the mobile app, which fetches the JSON from Firebase and
  starts play instantly.
- **Creator clear-check.** A stage can only be deployed if its creator
  beats it within the time limit — a proven quality filter.
- **AI-native creation.** Because stages are pure text, an AI can generate,
  validate, and remix them directly from natural-language prompts. That is
  the "Prompt" in Prompt World.
- **Future:** local P2P stage sharing between nearby players (StreetPass-style).

## Visual Identity

Strictly two colors: background `#000000`, all objects `#FFFFFF`.

## Repository Layout

```
promptworld/
├── README.md            # this file
├── docs/
│   └── stage-schema.md  # stage JSON format draft (source of truth for data design)
└── PromptWorld/         # Unity 6 (6000.5) project — all C# lives in Assets/Scripts,
                         # scene is generated by Assets/Editor/Day1SceneBuilder.cs
```

## Dev Log

- **Day 1 (2026-07-08):** Project kickoff. Core loop scaffold in Unity 6
  (2D): physics-based player (move + single jump), goal trigger with
  "Stage Clear!", 60s countdown with "Game Over" freeze. Stage JSON
  schema v0.1 drafted.
